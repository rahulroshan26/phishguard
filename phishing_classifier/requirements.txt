# PhishGuard — Python dependencies
# Install: pip install -r requirements.txt

flask>=3.0.0
requests>=2.31.0
Pillow>=10.0.0       # optional — for future image analysis

# Development / testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional enrichment (install separately if needed)
# python-whois>=0.8.0       # domain WHOIS lookups
# shodan>=1.31.0            # Shodan API integration
